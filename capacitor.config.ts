import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.futuretech.app',
  appName: 'Future Tech solution',
  webDir: 'dist'
};

export default config;
